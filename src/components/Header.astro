---
const currentPath = Astro.url.pathname;
const isTeamPage = currentPath === '/our-team';
---

<header>
    <h1><a href="/">DERİN AKADEMİ</a></h1>
    <navbar>
        {!isTeamPage && <button id="startSmall">HEMEN BAŞLA</button>}
    </nav>
</header>
